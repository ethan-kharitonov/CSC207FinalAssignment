package graph;

public class GraphGenerator {
//    public PlanarGraph generate(){
//
//        // generate cycle
//        PlanarGraph graph = new PlanarGraph();
//
//        float avgRotation = (float)Math.PI/5;
//        int avgRadius = 100;
//
//
//        float theta = 0;
//        float r = 0;
//
//        PlanarNode n0 = new PlanarNode<>(r*(float)Math.cos(theta), r*(float)Math.sin(theta));
//
//        while (theta < 2*Math.PI) {
//            theta += Math.random()*avgRotation;
//            if (theta < 2*Math.PI) {
//                r += (float) Math.random() * avgRadius;
//                PlanarNode<T> n1 = new PlanarNode<>(r * (float) Math.cos(theta), r * (float) Math.sin(theta));
//                graph.addNode(n1);
//                graph.joinUndirected(n0, n1);
//            }
//        }
//        return graph;
//    }

}
